name: Build and Push Web Docker Image to DigitalOcean

on:
  push:
    branches:
      - master
    paths:
      - "apps/web/**"
      - ".github/workflows/deployment_web.yml"

jobs:
  check-all:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - uses: ./.github/actions/setup-yarn
      - name: Run all checks
        run: yarn check:all

  get-version:
    runs-on: ubuntu-latest
    needs: check-all
    outputs:
      WEB_VERSION: ${{ steps.get_version.outputs.WEB_VERSION }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Get web version
        id: get_version
        run: echo "WEB_VERSION=$(jq -r .version apps/web/package.json)" >> $GITHUB_OUTPUT

  build-and-push:
    runs-on: ubuntu-latest
    needs: get-version
    env:
      WEB_VERSION: ${{ needs.get-version.outputs.WEB_VERSION }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to DigitalOcean Container Registry
        uses: docker/login-action@v2
        with:
          registry: ${{ secrets.DIGITALOCEAN_REGISTRY_URL }}
          username: ${{ secrets.DIGITALOCEAN_USERNAME }}
          password: ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: ./
          push: true
          tags: ${{ secrets.DIGITALOCEAN_REGISTRY_URL }}/corevia:web-${{ env.WEB_VERSION }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          file: ./apps/web/Dockerfile
          provenance: false

  deploy:
    runs-on: ubuntu-latest
    needs: [get-version, build-and-push]
    env:
      WEB_VERSION: ${{ needs.get-version.outputs.WEB_VERSION }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup kubectl
        uses: azure/setup-kubectl@v4

      - name: Log in to DigitalOcean Container Registry
        uses: docker/login-action@v2
        with:
          registry: ${{ secrets.DIGITALOCEAN_REGISTRY_URL }}
          username: ${{ secrets.DIGITALOCEAN_USERNAME }}
          password: ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}

      - name: Install doctl
        uses: digitalocean/action-doctl@v2
        with:
          token: ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}

      - name: Create kube directory
        run: mkdir -p ~/.kube

      - name: Get kubeconfig
        run: |
          doctl kubernetes cluster kubeconfig show corevia > ~/.kube/config

      - name: Update deployment image
        run: |
          kubectl set image deployment/corevia-web corevia-web=${{ secrets.DIGITALOCEAN_REGISTRY_URL }}/corevia:web-${{ env.WEB_VERSION }}

      - name: Wait for rollout
        run: |
          kubectl rollout status deployment/corevia-web --timeout=120s

      - name: Tag as web-latest (only if rollout succeeded)
        if: success()
        run: |
          docker pull registry.digitalocean.com/corevia/corevia:web-${{ env.WEB_VERSION }}
          docker tag registry.digitalocean.com/corevia/corevia:web-${{ env.WEB_VERSION }} \
                    registry.digitalocean.com/corevia/corevia:web-latest
          docker push registry.digitalocean.com/corevia/corevia:web-latest

      - name: Delete previous version tags
        continue-on-error: true
        if: success()
        run: |
          # Get all tags starting with "web-" except current and latest
          old_tags=$(doctl registry repository list-tags corevia \
            --format Tag \
            --no-header | grep '^web-' | grep -v "web-${{ env.WEB_VERSION }}" | grep -v "web-latest")

          # Track which digests we've already deleted
          deleted_digests=""

          for tag in $old_tags; do
            echo "Processing tag: $tag"

            # Get the digest for this tag
            digest=$(doctl registry repository list-tags corevia \
              --format Tag,Digest \
              --no-header | grep "^$tag" | awk '{print $2}')

            if [[ -z "$digest" ]]; then
              echo "No digest found for tag $tag, skipping."
              continue
            fi

            # Check if we already deleted this digest
            if echo "$deleted_digests" | grep -q "$digest"; then
              echo "Digest $digest already deleted, skipping."
              continue
            fi

            echo "Deleting manifest digest: $digest"
            doctl registry repository delete-manifest corevia $digest --force

            # Mark digest as deleted
            deleted_digests="$deleted_digests $digest"
          done
