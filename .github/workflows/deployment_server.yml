# Builds the server app Docker image and deploys it to the Kubernetes cluster on DigitalOcean.
# Triggered on every push to master that touches apps/server or this workflow file.
name: Build and Push Docker Server Image to DigitalOcean

on:
  push:
    branches:
      - master
    paths:
      - "apps/server/**"
      - ".github/workflows/deployment_server.yml"
  workflow_dispatch:

jobs:
  # Runs linting, type-checking, and any other workspace-level quality checks
  # before allowing the build to proceed.
  check-all:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 1
      - uses: ./.github/actions/setup-pnpm
      - name: Run all checks
        run: pnpm run check:all

  # Reads the semver version from apps/server/package.json and exposes it as a
  # job output so downstream jobs can tag the Docker image consistently.
  get-version:
    runs-on: ubuntu-latest
    needs: check-all
    outputs:
      SERVER_VERSION: ${{ steps.get_version.outputs.SERVER_VERSION }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Get server version
        id: get_version
        run: echo "SERVER_VERSION=$(jq -r .version apps/server/package.json)" >> $GITHUB_OUTPUT

  # Builds the server Dockerfile and pushes two tags to the DigitalOcean registry:
  #   - server-<version>  (immutable, version-pinned)
  #   - server-latest     (floating, always points to the newest build)
  # GitHub Actions cache is used to speed up layer reuse across runs.
  build-and-push:
    needs: get-version
    runs-on: ubuntu-latest
    env:
      SERVER_VERSION: ${{ needs.get-version.outputs.SERVER_VERSION }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to DigitalOcean Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ secrets.DIGITALOCEAN_REGISTRY_URL }}
          username: ${{ secrets.DIGITALOCEAN_USERNAME }}
          password: ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: ./
          push: true
          tags: |
            ${{ secrets.DIGITALOCEAN_REGISTRY_URL }}/corevia:server-${{ env.SERVER_VERSION }}
            ${{ secrets.DIGITALOCEAN_REGISTRY_URL }}/corevia:server-latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
          target: server
          file: ./apps/server/Dockerfile
          provenance: false

  # Updates the running Kubernetes deployment to use the newly pushed image,
  # then cleans up old tags and dangling manifests from the registry.
  deploy:
    runs-on: ubuntu-latest
    needs: [get-version, build-and-push]
    env:
      SERVER_VERSION: ${{ needs.get-version.outputs.SERVER_VERSION }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      # kubectl is needed to interact with the DigitalOcean Kubernetes cluster.
      - name: Setup kubectl
        uses: azure/setup-kubectl@v4

      - name: Log in to DigitalOcean Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ secrets.DIGITALOCEAN_REGISTRY_URL }}
          username: ${{ secrets.DIGITALOCEAN_USERNAME }}
          password: ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}

      # doctl is the DigitalOcean CLI used to manage the registry and fetch the kubeconfig.
      - name: Install doctl
        uses: digitalocean/action-doctl@v2
        with:
          token: ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}

      - name: Create kube directory
        run: mkdir -p ~/.kube

      # Writes the cluster credentials into ~/.kube/config so kubectl can authenticate.
      - name: Get kubeconfig
        run: |
          doctl kubernetes cluster kubeconfig show corevia > ~/.kube/config

      # Performs a rolling update of the corevia-server deployment to the new image version.
      # Kubernetes will drain old pods only after the new ones pass their readiness probes.
      - name: Update deployment image
        run: |
          kubectl set image deployment/corevia-server corevia-server=${{ secrets.DIGITALOCEAN_REGISTRY_URL }}/corevia:server-${{ env.SERVER_VERSION }}

      # Removes obsolete server-* tags and untagged (dangling) manifests from the registry
      # to keep storage usage in check. Failures are non-blocking (continue-on-error).
      - name: Delete previous version tags
        continue-on-error: true
        if: success()
        run: |
          all_tags=$(doctl registry repository list-tags corevia --format Tag --no-header)
          echo "All tags found: $all_tags"

          old_tags=$(echo "$all_tags" | grep '^server-' | grep -v "server-${{ env.SERVER_VERSION }}" | grep -v "server-latest" || true)
          echo "Old tags to delete: ${old_tags:-none}"

          deleted_digests=""

          for tag in $old_tags; do
            echo "Processing tag: $tag"

            digest=$(doctl registry repository list-tags corevia \
              --format Tag,Digest \
              --no-header | grep "^$tag[[:space:]]" | awk '{print $2}' || true)

            if [[ -z "$digest" ]]; then
              echo "No digest found for tag $tag, skipping."
              continue
            fi

            if echo "$deleted_digests" | grep -qF "$digest"; then
              echo "Digest $digest already deleted, skipping."
              continue
            fi

            echo "Deleting manifest for tag $tag (digest: $digest)"
            doctl registry repository delete-manifest corevia "$digest" --force

            deleted_digests="$deleted_digests $digest"
          done

          echo "Deleting untagged manifests..."
          untagged_digests=$(doctl registry repository list-manifests corevia \
            --format Digest,Tags \
            --no-header | grep '\[\]' | awk '{print $1}' | sort | uniq || true)
          echo "Untagged digests found: ${untagged_digests:-none}"

          for digest in $untagged_digests; do
            if [[ -z "$digest" ]]; then
              continue
            fi

            if echo "$deleted_digests" | grep -qF "$digest"; then
              echo "Digest $digest already deleted, skipping."
              continue
            fi

            echo "Deleting untagged manifest (digest: $digest)"
            doctl registry repository delete-manifest corevia "$digest" --force

            deleted_digests="$deleted_digests $digest"
          done
